import { Database, Eraser, HardDrive, RotateCcw } from 'lucide-react';
import type { ArchiveSourceProgress } from '../../types';
type Props={ total:number; sources:ArchiveSourceProgress[]; cacheHits:number; cacheEnabled:boolean; onClear:()=>void; onRefresh:()=>void };
export function DataManager({total,sources,cacheHits,cacheEnabled,onClear,onRefresh}:Props){const ready=sources.filter(source=>source.state==='ready').length;return <section id="veri-yonetimi" className="data-manager" aria-labelledby="data-manager-title"><div><p className="eyebrow"><Database size={14}/> VERİ YÖNETİMİ</p><h2 id="data-manager-title">Canlı arşiv, kontrol sende.</h2><p>SÉRA yalnızca o an istediğin TMDB sonuç sayfasını alır ve kısa süreli cihaz önbelleğinde tutar. Tüm katalog indirilmez; kişisel listelerin ve tercihlerinin hiçbirisi sunucu hesabına yazılmaz.</p></div><div className="data-manager-stats"><span><HardDrive size={16}/><b>{total.toLocaleString('tr-TR')}</b><small>canlı sonuç</small></span><span><RotateCcw size={16}/><b>{cacheEnabled ? cacheHits : '—'}</b><small>önbellek isabeti</small></span><span><Database size={16}/><b>{ready}/{sources.length}</b><small>hazır kaynak</small></span></div><div className="data-manager-actions"><button onClick={onRefresh}><RotateCcw size={16}/> Sonuçları yenile</button><button className="danger" onClick={onClear}><Eraser size={16}/> Kaynak önbelleğini temizle</button></div></section>}
