import { Command, Dice5, Heart, Menu, Search, Sparkles } from 'lucide-react'; import { useEffect, useRef } from 'react';
export function Navbar({query,setQuery,onWatchlist,onRandom,onMood}:{query:string;setQuery:(v:string)=>void;onWatchlist:()=>void;onRandom:()=>void;onMood:()=>void}){const input=useRef<HTMLInputElement>(null);useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();input.current?.focus();}};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[]);return <header className="sticky top-0 z-40 border-b border-line bg-cinema/85 backdrop-blur-xl"><nav className="mx-auto flex h-16 max-w-7xl items-center gap-3 px-4"><a className="flex items-center gap-2 font-black text-white" href="#top"><span className="grid h-8 w-8 place-items-center rounded-lg bg-gradient-to-br from-red-500 to-violet-500">▶</span><span className="sera-word">SÉRA</span></a><div className="hidden flex-1 md:block"><div className="command-search"><Search size={16}/><input ref={input} value={query} onChange={e=>setQuery(e.target.value)} placeholder="Dizi veya duygu ara…"/><kbd><Command size={11}/>K</kbd></div></div><button className="icon-button hidden sm:flex" aria-label="Ruh haline göre keşfet" onClick={onMood}><Sparkles size={18}/></button><button className="icon-button" aria-label="Rastgele dizi seç" onClick={onRandom}><Dice5 size={19}/></button><button className="icon-button relative" aria-label="İzleme listeni aç" onClick={onWatchlist}><Heart size={19}/><span className="notification">•</span></button><button className="icon-button md:hidden" aria-label="Menü"><Menu size={19}/></button></nav></header>}
