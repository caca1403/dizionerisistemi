import { motion } from 'framer-motion'; import { ArrowRight, Sparkles } from 'lucide-react'; import type { MoodTag } from '../../types'; import { MoodChips } from './MoodChips';
export function HeroSearch({query,setQuery,mood,onMood,scrollToDiscovery,onGenerate}:{query:string;setQuery:(v:string)=>void;mood:MoodTag|'Tümü';onMood:(m:MoodTag)=>void;scrollToDiscovery:()=>void;onGenerate:()=>void}){return <section className="hero-section relative overflow-hidden border-b border-line py-16 sm:py-24"><div className="hero-scene" aria-hidden="true"/><div className="hero-vignette" aria-hidden="true"/><div className="hero-glow"/><div className="relative mx-auto max-w-7xl px-4"><motion.p initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="eyebrow"><Sparkles size={14}/> Kişisel izleme zevki</motion.p><h1 className="mt-5 max-w-4xl text-4xl font-black tracking-tight text-white sm:text-6xl lg:text-7xl">Sıradan listeleri unut.<br/><span className="gradient-text">Tam olarak ne hissetmek istiyorsan,</span> onu izle.</h1><p className="mt-6 max-w-2xl text-base leading-7 text-slate-300 sm:text-lg">SÉRA; duygu, tempo, anlatı derinliği ve benzerlik sinyalleriyle sana göre bir dizi rotası çıkarır.</p><div className="terminal mt-8 max-w-4xl"><label htmlFor="hero-search" className="sr-only">Doğal dil ile dizi ara</label><input id="hero-search" value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==='Enter'&&onGenerate()} placeholder="Örn: Dark gibi gizemli ama daha hızlı bir dizi öner…"/><button onClick={onGenerate}><Sparkles size={18}/> Öneri üret <ArrowRight size={17}/></button></div><div className="mt-5"><MoodChips active={mood} onPick={onMood}/></div></div></section>}
